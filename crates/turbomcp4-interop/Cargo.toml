[package]
name = "turbomcp4-interop"
version = "4.0.0-alpha.1"
edition = "2024"
rust-version = "1.85"
description = "Cross-SDK interop smoke tests: turbomcp4 ↔ rmcp (the official Rust MCP SDK)."
license = "MIT"
publish = false

# NOTE: this crate is `exclude`d from the parent workspace (see the root
# Cargo.toml) so the heavy `rmcp` dependency tree stays out of the main
# lockfile/gate. Run it directly: `cd crates/turbomcp4-interop && cargo test`.

[dependencies]

[dev-dependencies]
turbomcp4 = { path = "../turbomcp4", features = ["client", "http"] }
turbomcp4-service = { path = "../turbomcp4-service" }
turbomcp4-transport-stdio = { path = "../turbomcp4-transport-stdio" }
turbomcp4-core = { path = "../turbomcp4-core", features = ["std"] }
# The official Rust MCP SDK, from crates.io — interop is tested against the real
# released SDK, not a local checkout. rmcp's LATEST is 2025-11-25, which is
# turbomcp4's legacy path; interop runs there.
rmcp = { version = "1.7", features = ["client", "server", "macros"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "io-util", "sync"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
criterion = { version = "0.8", features = ["html_reports", "async_tokio"] }

[[bench]]
name = "sdk_comparison"
harness = false
