//! TurboMCP v4 codegen tool — Phase 0 skeleton.
//!
//! Phase 0.5 wires up `typify` to turn
//! `reference/modelcontextprotocol/schema/{2025-11-25,draft}/schema.json`
//! into checked-in `turbomcp4-protocol::v*::types` modules.
#![forbid(unsafe_code)]

fn main() {
    eprintln!("turbomcp4-codegen: not yet implemented (Phase 0.5)");
    std::process::exit(2);
}
