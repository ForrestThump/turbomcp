//! Tests for the `TransportMetrics::metadata` field.
//!
//! These tests validate the `metadata` field on [`crate::core::TransportMetrics`]
//! that allows transport-specific custom metrics to be stored without breaking
//! the core metrics API.

#[cfg(test)]
mod tests;
